name: "Android Build (GitHub-only: prebuild + Gradle)"

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      JAVA_HOME: /usr/lib/jvm/temurin-17-jdk-amd64
      CI: 1

    steps:
      # 📦 1. Получаем код
      - name: Checkout
        uses: actions/checkout@v4

      # ⚙️ 2. Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci || npm i

      # ☕️ 3. Java 17
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      # 🤖 4. Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # ✅ Автопринятие лицензий
      - name: Accept Android SDK licenses
        run: yes | sdkmanager --licenses || true

      # 🖼️ Генерация иконок, если их нет
      - name: Ensure app icons exist
        run: |
          mkdir -p assets
          sudo apt-get update && sudo apt-get install -y imagemagick
          if [ ! -s assets/icon.png ]; then
            convert -size 512x512 xc:"#C9A574" -gravity center -pointsize 180 -fill white -annotate 0 "BAR" assets/icon.png
            echo "✅ Generated assets/icon.png"
          fi
          if [ ! -s assets/adaptive-icon.png ]; then
            convert -size 512x512 xc:"#C9A574" -gravity center -pointsize 180 -fill white -annotate 0 "BAR" assets/adaptive-icon.png
            echo "✅ Generated assets/adaptive-icon.png"
          fi

      # ⚙️ Expo Prebuild
      - name: Prebuild (generate android/)
        env:
          CI: 1
        run: npx expo prebuild --platform android

      # 🩹 ФИКС: убираем file("") из app/build.gradle (основная причина краша)
      - name: Patch build.gradle entryFile
        run: |
          FILE=android/app/build.gradle
          echo "🔧 Checking for 'entryFile = file(''...)' issues in $FILE"
          # Удаляем строку, которую expo добавляет с пустым file('')
          sed -i "/entryFile\s*=\s*file(/d" "$FILE"
          # Добавляем безопасную конфигурацию entryFile
          echo "" >> "$FILE"
          echo "// --- CI patch: force entryFile to avoid file('') crash" >> "$FILE"
          echo "project.ext.set(\"react\", [" >> "$FILE"
          echo "  entryFile: \"${GITHUB_WORKSPACE}/index.js\"" >> "$FILE"
          echo "])" >> "$FILE"
          echo "✅ build.gradle patched successfully."

      # 🔐 Декодирование keystore из base64
      - name: Decode keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/app/my-release-key.jks
          echo "✅ Keystore decoded"

      # 🧩 Настройка signing-конфига для Gradle
      - name: Configure signing (gradle.properties)
        run: |
          echo "MYAPP_UPLOAD_STORE_FILE=$PWD/android/app/my-release-key.jks" >> android/gradle.properties
          echo "MYAPP_UPLOAD_KEY_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }}" >> android/gradle.properties
          echo "MYAPP_UPLOAD_STORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" >> android/gradle.properties
          echo "MYAPP_UPLOAD_KEY_PASSWORD=${{ secrets.ANDROID_KEY_ALIAS_PASSWORD }}" >> android/gradle.properties

      # 🧱 Проверка наличия keystore
      - name: Verify keystore exists
        run: |
          if [ ! -f android/app/my-release-key.jks ]; then
            echo "❌ Keystore file missing!"
            exit 1
          else
            echo "✅ Keystore file verified."
          fi

      # 🏗️ Сборка (AAB + APK)
      - name: Build Release (AAB + APK)
        working-directory: android
        run: |
          ./gradlew clean
          ./gradlew bundleRelease --no-daemon --stacktrace
          ./gradlew assembleRelease --no-daemon --stacktrace

      # 📦 Загрузка артефактов
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: |
            android/app/build/outputs/**/*.aab
            android/app/build/outputs/**/*.apk
            android/app/build/outputs/mapping/release/mapping.txt
