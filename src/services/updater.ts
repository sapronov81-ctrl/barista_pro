import * as FileSystem from 'expo-file-system'; export async function checkForUpdates(){return {version:'1.1.1',min_app:'1.0.0',date:new Date().toISOString(),notes:'Demo'};} export async function downloadAndActivateFake(){const dir=FileSystem.documentDirectory+'content/v1_1_1/';await FileSystem.makeDirectoryAsync(dir,{intermediates:true});await FileSystem.writeAsStringAsync(dir+'manifest.json',JSON.stringify({version:'1.1.1'}));return dir;}